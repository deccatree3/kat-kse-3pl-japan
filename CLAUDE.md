# KAT-KSE 3PL Japan - 일본 현지 물류비 관리

## 프로젝트 개요
KATCHERS, INC의 일본 현지 3PL(국제익스프레스, KOKUSAI EXPRESS) 물류비 내역을 검토하고 히스토리를 관리하는 프로젝트.

## ⚠️ Qoo10 출고 기능 제거 완료 + 공유 Supabase 주의 (필독)

**Qoo10 출고 기능은 자매 프로젝트 `C:\claude\kat-outbound-hub`(캐처스/네뉴 출고 통합)
로 이식·확장 완료** → 이 프로젝트에서는 **코드 제거 완료 (2026-05-22)**. 단, 코드는 지웠어도
**공유 DB 테이블·인스턴스는 보존**해야 한다 (outbound-hub 가 계속 사용).

이 프로젝트의 **Supabase(Postgres) 는 outbound-hub 와 같은 인스턴스를 공유** (Free 한도
제약). 양쪽 `config.json` 의 `database_url` 이 **동일 DSN** 확인 (2026-05-22).
> 로컬 SQLite `db/logistics.db`(물류비) 는 이 프로젝트 전용 — 공유 아님.

### 제거 완료된 것 (2026-05-22, Qoo10 출고 — outbound-hub 로 이전됨)
- `qoo10/` 폴더 전체 삭제 (`api_client.py`, `generator.py`, `templates/`, `_archive/`) — 독립 사본이 outbound-hub 에 있음.
- `dashboard.py` 의 Qoo10 메뉴 옵션 / 사이드바 자격증명 expander / 출고요청 본체 제거 (qoo10·QSM 참조 **144건 → 0**, **2183 → 1096행**). 분기 패턴이 `if menu == X: … st.stop()` 라 물류비·재고 메뉴는 fallthrough 로 무손상.
- `config.json` 의 `qoo10_api_key`/`qoo10_user_id`/`qoo10_password` 제거. `.gitignore` 의 `qoo10/_archive/` 와 `.claude/settings.local.json` 의 qoo10 권한 항목 정리.
- ⚠️ **공유 DB 테이블·DSN 은 의도적으로 미변경** (아래 "절대 건드리면 안 되는 것" 준수).

### 절대 건드리면 안 되는 것 (= outbound-hub 가 깨짐)
1. **Supabase 프로젝트 삭제/일시정지** — 두 프로젝트 DB 통째 소멸.
2. 공유 테이블 **drop / rename / 파괴적 변경**:
   - `qoo10_credentials`, `qoo10_product_mapping`, `qoo10_outbound`, `qoo10_pending_brief`
     — Qoo10 코드 제거 후엔 **outbound-hub 전용**. 테이블 자체는 **반드시 보존**(drop 금지).
3. **DSN/비밀번호 변경** 시 → outbound-hub `config.json` + Streamlit Cloud secrets 도 같이 갱신.

### 유지 대상 (Qoo10 아님 — 이 프로젝트의 재고/물류 기능)
- `db/stock_loader.py`, `alerts/notifier.py` — Qoo10 참조 0건. `shipments`/`stock_snapshots`
  (재고 소진 예측) 적재. outbound-hub 는 이 두 테이블을 SKU 카탈로그 **fallback**(2순위)으로만 사용.
- SQLite `logistics.db` + 물류비 검토 기능 일체.

(outbound-hub 세션의 두 프로젝트 의존성 점검 결과로 추가, 2026-05-22. 같은 날 Qoo10 코드 제거 완료.)

## 3PL 업체 정보
- **업체명**: 株式会社国際エキスプレス (KOKUSAI EXPRESS CO., LTD.)
- **담당자**: 金 容度 (김용도)
- **소재지**: 東京都大田区東海4-2-3 国際エキスプレス東京第二倉庫 5階
- **TEL**: 03-6457-0788 / FAX: 03-6412-9351
- **사업자번호**: T7011101027536
- **거래은행**: 三井住友銀行 新宿通支店, 普通 8201374

## 폴더 구조

```
raw/                     # 원본 자료
  재고현황 내역_*.xlsx   # 현시점 재고 스냅샷 (월별이 아닌 시점 데이터)
  YYYYMM/               # 월별 하위폴더 ex) 202603/
    # --- KSE 제공 (3PL측 정산 자료) ---
    *_詳細_*.xlsx        # KSE 작업 상세 (입고, 출하, 집계 등)
    *_請求書.pdf         # 청구서 (공식 인보이스)
    *_請求内訳確認書.pdf  # 청구내역 확인서 (항목별 상세)
    *銀行情報*.pdf       # 은행 정보 (송금용)
    # --- KATCHERS 제공 (우리측 대조 자료, KSE OMS에서 다운로드) ---
    ORDER_LIST_B2C_*.xlsx       # B2C 오더리스트 (상품 라인별 상세: 상품코드, 수량, 수취인 등)
    ORDER_LIST_B2B_*.xlsx       # B2B 오더리스트 (상품 라인별 상세)
    주문(출고&입고) 내역_*B2C.xlsx  # B2C 주문 내역 (주문 단위 요약: 작업상태, 내부오더번호 등)
    주문(출고&입고) 내역_*B2B.xlsx  # B2B 주문 내역 (주문 단위 요약)

db/                      # 데이터베이스
  logistics.db           # SQLite DB (모든 물류비 데이터)
  init_db.py             # DB 스키마 생성 + 초기 데이터 적재
  loader.py              # 월별 raw 파일 파싱/검증 유틸리티

analysis/                # 분석 리포트 (월별)
  YYYYMM_review.md      # 검토 결과 리포트

history/                 # 히스토리 CSV (백업/참조용)
  monthly_summary.csv   # 월별 요약
  unit_price.csv        # 단가표
  line_items.csv        # 항목별 상세

dashboard.py             # Streamlit 대시보드
```

## 대시보드 실행

```bash
streamlit run dashboard.py
```

대시보드 탭 구성:
1. **월간 요약** - KPI, 비용 구성비 (파이/바 차트)
2. **항목 상세** - 26개 항목별 청구/검증 대조표 (판정 색상 표시)
3. **검토 결과** - 불일치/확인필요 사항 목록, 금액 영향 요약
4. **추이 분석** - 월별 청구 추이, 출하건수/건당비용 (2개월 이상 시)
5. **단가표** - 현행 단가 목록 및 분포 차트

## DB 테이블 구조

- `invoices` - 월별 청구서 (인보이스 단위)
- `line_items` - 항목별 상세 (청구 라인 26개)
- `unit_prices` - 단가 이력 (변동 추적)
- `monthly_metrics` - 월별 운영 지표
- `review_findings` - 검토 결과 (불일치/확인필요)

## 월별 물류비 검토 절차

1. **raw 자료 수집**: `raw/YYYYMM/` 폴더에 원본 파일 배치
2. **수량 대조**: Excel 상세 데이터와 청구내역 확인서의 수량 비교
3. **금액 검증**: 단가 × 수량 = 청구금액 일치 여부 확인
4. **소비세 검증**: 소계 × 10% = 소비세 일치 확인
5. **불일치 기록**: 차이 발견 시 analysis 리포트에 기록
6. **히스토리 갱신**: history CSV 파일에 해당 월 데이터 추가

## KATCHERS 제공 파일 설명 (매월 4개)

| 파일 | 내용 | 1행=1건 기준 | 핵심 컬럼 |
|------|------|-------------|-----------|
| ORDER_LIST_B2C | 상품 라인별 상세 | **상품** (주문 1건에 여러 행) | 상품코드, 상품명, 예정수량, 수취인주소, 송장번호 |
| ORDER_LIST_B2B | 상품 라인별 상세 | **상품** | 상품코드, 상품명, 예정수량, 판매처 |
| 주문 내역 B2C | 주문 단위 요약 | **주문** (1건=1행) | 내부오더번호, 작업상태, 취소여부, 택배사, 입력자 |
| 주문 내역 B2B | 주문 단위 요약 | **주문** | 내부오더번호, 작업상태, 취소여부 |

- ORDER_LIST: 어떤 **상품**이 몇 개 출고되었는지 (정산 수량 대조용)
- 주문 내역: 주문의 **처리 상태**와 KSE 내부 오더번호 확인용
- 내부 오더번호(`260331OR000018` 형식) = KSE WMS의 주문 코드

## 주요 검증 포인트

### KATCHERS vs KSE 대조 (3-way 검증)
- **B2C 주문 건수**: KATCHERS 주문내역 vs KSE 집계(B2C出荷集計) vs 청구서
- **피킹 PCS**: ORDER_LIST 총 수량 vs KSE 집계 PCS vs 청구서
- **송장번호 매칭**: ORDER_LIST 송장 vs KSE 집계 송장 (누락 건 특정)

### KSE 내부 검증
- **배송 사이즈별 건수**: 집계 시트 사이즈별 집계 vs 확인서 배송료 건수
- **오키나와/낙도 중계료**: 개별 주문 중계료 합산 vs 확인서 중계료
- **입고 수량**: 入庫 시트 합계 vs 확인서 입고/재고등록 수량
- **보관료**: PLT 수 별도 확인 필요 (Excel에 월간 보관 데이터 없음)
- **단가 변동**: unit_price.csv 대비 단가 변경 여부

## 청구 항목별 산출식 (개수의 의미)

| No | 작업내용 | 단가 | "개수"의 의미 | 산출 소스 (KSE 엑셀) |
|----|----------|------|-------------|---------------------|
| 1 | 입고,재고등록(PLT) | 980 | 입고 팔레트 수 | 入庫: 入庫PLT数 합계 |
| 2 | 입고,재고등록(CTN) | 95 | 입고 카톤 수 | 入庫: 入庫CTN数 합계 |
| 3 | 입고,재고등록(PCS) | 10 | 입고 낱개 수 | 入庫: 入庫pcs数 합계 |
| 4 | 보관료 | 2,100 | 보관 중인 팔레트 수 | **근거 없음** (SKU별 로케이션 추정) |
| 5 | 피킹요금_PCS | 9 | B2C 출고 제품 수량 합계 (총 PCS) | B2C出荷集計: 確定数量 합계 |
| 6 | 피킹요금_CTN | 95 | B2B 출하 카톤 수 | B2B出荷: 출하수량CTN 합계 |
| 7 | B2C 출하작업수수료 | 85 | B2C 출하 건수 (=유니크 송장번호 수) | B2C出荷集計: No. 카운트 |
| 8 | B2B 출하작업수수료 | 125 | B2B 출하 건수 | B2B出荷: No 카운트 |
| 9 | 완충자재 | 30 | B2C 출하 건수 (=No.7과 동일) | B2C出荷集計: No. 카운트 |
| 10 | 세트작업비용 | 300 | 세트 구성 작업 횟수 | - |
| 11 | 라벨링 작업 | 20 | 라벨 부착 매수 (B2B PO/송장 라벨) | B2B出荷: PO.송장 라벨작업 합계 |
| 12 | Repalletizing | 1,200 | 재파레타이징 팔레트 수 | - |
| 13 | 출하작업비용_PLT | 1,200 | 트럭 적재 팔레트 수 | - |
| 14-17 | 포장박스 (사이즈별) | 46~410 | 해당 사이즈 박스 사용 수 (=해당 사이즈 출하 건수) | B2C出荷集計: 출고사이즈별 카운트 |
| 18-22 | B2C 배송료 (사이즈별) | 450~1,250 | 해당 사이즈 배송 건수 (=No.14-17과 동일) | B2C出荷集計: 출고사이즈별 카운트 |
| 23 | 오키나와 중계료 | 7,950 | 일괄(1식) | B2C出荷集計: 중계료 컬럼 합계 |
| 24-26 | 오키나와 배송료 | 1,330~2,430 | 오키나와향 사이즈별 건수 | B2C出荷集計: 오키나와 컬럼 |

핵심 관계:
- **출하 건수** (No.7,9,14~22) = 유니크 송장번호 수 = 배송 박스 수
- **제품 수량** (No.5) = 총 상품 PCS (1주문에 2PCS면 2로 카운트)
- **포장/배송** (No.14~22) = 출하 건수를 사이즈별로 분해한 것
- No.7(출하수수료) = No.9(완충자재) = No.14~17 합계(포장) = No.18~22 합계(배송)

## 참고사항

- 소비세: 경감세율(8%) 대상 없음, 모두 표준세율(10%) 적용
- 청구서 2종류: ① 월 정기 물류비 (확인서 기반) ② 별도 청구 (수시)
- 지급기한: 매월 말일 청구 → 익월 말일까지 송금
