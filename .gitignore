# Sensitive config - NEVER commit (webhook URL, database URL)
alerts/config.json
config.json

# Raw files (주문/재고 원본, 개인정보 포함 가능)
raw/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# SQLite 보조 파일 (WAL 모드)
*.db-shm
*.db-wal

# Claude Code local settings
.claude/

# Streamlit (secrets만 제외, config.toml은 포함)
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
